{
  "name": "buswhensg-live",
  "private": true,
  "version": "0.1.0",
  "description": "Local live-data test harness for BusWhenSG (Cloudflare Pages dev + LTA DataMall proxy)",
  "scripts": {
    "dev": "wrangler pages dev public --port 8788"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
